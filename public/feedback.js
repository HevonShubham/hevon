"use strict";
const form=document.getElementById("feedback-form");
const opt=document.getElementById("research-opt-in"),email=document.getElementById("email"),emailFields=document.getElementById("email-fields"),button=document.getElementById("submit"),error=document.getElementById("form-error");
const profileConsent=document.getElementById("profile-consent"),nutritionFields=document.getElementById("nutrition-fields");
function sync(){nutritionFields.hidden=!profileConsent.checked;nutritionFields.disabled=!profileConsent.checked;emailFields.hidden=!opt.checked;email.disabled=!opt.checked;email.required=opt.checked;}
opt.addEventListener("change",sync);profileConsent.addEventListener("change",sync);sync();
form.addEventListener("submit",async event=>{event.preventDefault();if(button.disabled)return;if(!form.reportValidity())return;const id=document.getElementById("submission-id");if(!id.value)id.value=crypto.randomUUID();const data=new FormData(form);if(!profileConsent.checked){data.set("profileConsent","false");for(const field of ["goal","diet","challenge"])data.set(field,"");}if(!opt.checked){data.set("email","");data.set("researchOptIn","false");}button.disabled=true;button.textContent="Sending…";error.hidden=true;try{const response=await fetch("/feedback.html",{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body:new URLSearchParams(data).toString()});if(!response.ok)throw new Error();window.location.assign("/feedback-thanks.html");}catch{error.textContent="Your feedback couldn’t be sent. Please try again.";error.hidden=false;button.disabled=false;button.textContent="Send feedback →";}});
